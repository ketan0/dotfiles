- defaults:
    link:
        create: true
        relink: true
- clean: ['~']
- link:
    ~/.emacs.d/:
    ~/.gitconfig:
    ~/.config/karabiner.edn:
    ~/.ssh/config: sshconfig
    ~/.zshrc:
- shell:
    - [git submodule update --init --recursive, Installing submodules]
